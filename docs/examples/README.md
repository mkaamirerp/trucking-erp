# API request examples

These files are sample request payloads for manual testing and integration reference.

Replace IDs, dates, rates, and business-specific values before using them against any environment.
