# Developer / CI test dependencies (optional prod image; installed in Dockerfile `dev` stage only).
# Keep versions aligned with a known-good toolchain (see requirements.lock.txt for pytest when regenerating lock).
pytest==9.0.2
pytest-asyncio==1.3.0
